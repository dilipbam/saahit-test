from utilities.schemas.models import Base
from utilities.schemas.subscription_models import Base
from utilities.schemas.log_models import Base
from utilities.schemas.schedule_availability_models import Base
from utilities.schemas.base import Base

# all bases are imported as an array list here
all_bases = [Base]
